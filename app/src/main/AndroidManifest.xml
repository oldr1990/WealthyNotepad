<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wealthynotepad">
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>
    <application
        android:name="com.example.wealthynotepad.NotepadApplication"
        android:allowBackup="true"
        android:icon="@drawable/wealthy_notepad_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/wealthy_notepad_icon"
        android:supportsRtl="true"
        android:theme="@style/LoadScreen">
        <activity
            android:name="com.example.wealthynotepad.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/LoadScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>